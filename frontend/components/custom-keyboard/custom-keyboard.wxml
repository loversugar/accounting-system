<view class="keyboard">
	<view class="top-input">
		<text class="iconfont icon-beizhuyitianxie"/>

		<input
		 class="note"
		 placeholder="点击写备注..."
		/>
		<view class="money">0.00</view>
	</view>
	<view
	 style="grid-auto-columns: minmax({{keyWidth}}px, auto);grid-auto-rows: minmax({{keyHeight}}px, auto)"
	 class="board"
	>
		<block
		 wx:key="index"
		 wx:for="{{boardKeys}}"
		>
			<view class="key-content">{{item.number}}</view>
		</block>
	</view>
</view>

