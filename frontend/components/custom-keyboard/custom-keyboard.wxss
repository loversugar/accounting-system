@import "/style/iconfont.wxss";

.top-input {
    display: flex;
}

.board {
    width: inherit;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    grid-auto-rows: minmax(100px, auto);
}

.key-content {
    text-align: center;
    padding: 50px 10px;
}