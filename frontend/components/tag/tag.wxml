<view
 class="nav"
 style="width:{{width}}px"
>
	<i
	 style="font-size:30px;background-color:{{tag.iconfontBackgroundColor ? tag.iconfontBackgroundColor : '#f1f1f1'}}"
	 class="iconfont {{tag.categoryUrl}} small-tag"
	/>
	<text class="item-name">{{tag.categoryName}}</text>
</view>

