<view
 class="nav"
 style="width:{{width}}px"
 bind:tap="click"
>
	<i
	 style="font-size:30px;background-color:{{tag.isClick ? '#00b38a' : '#f1f1f1'}}"
	 class="iconfont {{tag.iconfontName}} small-tag"
	/>
	<view>{{tag.isClick}}</view>
	<text class="item-name">{{tag.name}}</text>
</view>

